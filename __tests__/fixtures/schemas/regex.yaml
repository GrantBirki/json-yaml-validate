type: object
properties:
  name:
    type: string
  id:
    type: string
    pattern: "^[0-9a-fA-F-]{36}$"
  tags:
    type: object
    properties:
      environment:
        type: string
    required:
      - environment
  owner:
    type: string
required:
  - name
  - id
  - tags
  - owner
